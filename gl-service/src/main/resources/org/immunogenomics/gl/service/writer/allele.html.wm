<!DOCTYPE html>
<html lang="en">
  <head>
    <title>${allele.Id}</title>
  </head>
  <body>
    <p class="type"><a href="http://immunogenomics.org/gl-ontology/Allele">http://immunogenomics.org/gl-ontology/Allele</a></p>
    <p class="title">${allele.Id}</p>
    <p class="identifier">${allele.Id}</p>
    <p class="label">${allele.Id}</p>
    <p class="comment">An allele with#if (${allele.Accession} != "") accession ${allele.Accession} and#end locus ${allele.Locus.Glstring} represented by the glstring ${allele.Glstring}</p>
    <p class="hasAccession">${allele.Accession}</p>
    <p class="hasGlstring">${allele.Glstring}</p>
    <p class="hasLocus"><a href="http://immunogenomics.org/gl-ontology/Locus">http://immunogenomics.org/gl-ontology/Locus</a> <a href="${allele.Locus.Id}.html">${allele.Locus.Id}</a></p>
  </body>
</html>
